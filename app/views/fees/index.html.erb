<div class="navbar">
    <div class="navbar-logo">
        <p>Logo</p>
    </div>
    <div class="navbar-title">
        <p>－日常に旅の学びと喜びを－</p>
        <h2><span>T</span>ravel <span>R</span>ecurrent <span>S</span>chool</h2>
    </div>
    <div class="toggle-btn">
        <i class="fas fa-bars"></i>
    </div>
    <div class="navbar-list">
        <ul>
            <li><%= link_to "TOP", "/" %></li>
            <% if user_signed_in? %>
              <li>／</li>
              <li><%= link_to "LOG AUTO", destroy_user_session_path, :method => "delete", data: {confirm:"ログアウトしますか？"} %></li>
              <li>／</li>
              <li><%= link_to "ADMIN", "/admins" %></li>
            <% end %>
        </ul>
    </div>
</div>

<div class="fee-index">
    <div class="fee-index-title">
        <h1>学費一覧（編集用）</h1>
    </div>
    <div class="fee-index-new-btn">
        <%= link_to "新規設定", "/fees/new" %>
    </div>
    <div class="fee-index-back-btn">
        <%= link_to "管理者編集一覧に戻る", "/admins" %>
    </div>
    <!--<div class="fee-index-main">-->
        <% if @fees.present? %>
          <% @fees.each do |f| %>
            <div class="fee-index-content">
                <% if f.release == "1" %>
                  <div class="release">
                      <p>掲載中</p>
                  </div>
                <% else %>
                  <div class="unrelease">
                      <p>非掲載</p>
                  </div>
                <% end %>
                <div class="fee-index-name">
                   <%= f.name %>
                </div>
                <div class="fee-index-price">
                    <%= f.price.to_s(:delimited) %>円
                </div>
                <div class="fee-index-text">
                    <%= f.text %>
                </div>
                </div>
                <div class="fee-index-btn-area">
                    <ul>
                        <li><%= link_to "編集", "/fees/#{f.id}/edit" %></li>
                        <li><%= link_to "削除", "/fees/#{f.id}", :method => "delete", data: {confirm: "削除してもよろしいですか？"} %></li>
                    </ul>
                </div>
            </div>
          <% end %>
        <% else %>
          <div class="fee-index-empty">
              <p>まだひとつも作成されていません。</p>
          </div>
        <% end %>
    <!--</div>-->
</div>